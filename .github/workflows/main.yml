name: Cron

on:
  schedule:
    - cron: "0 * * * *"

defaults:
  run:
    working-directory: ./target/release

jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
      - name: Run rust cron
        run: ./playground
