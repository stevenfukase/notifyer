name: Cron

on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches: [ "master" ]

defaults:
  run:
    working-directory: ./target/release

jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
      - name: Run rust cron
      - uses: actions/checkout@v2
        run: playground
