query SingleDayContribution($login: String!, $date: DateTime!) {
  user(login: "stevenfukase") {
    contributionsCollection(from: $date, to: $date) {
      contributionCalendar {
        weeks {
          contributionDays {
            contributionCount
            date
          }
        }
      }
    }
  }
}